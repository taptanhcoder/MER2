model:
  name: late-logit-avg
  fusion_dim: 128
  num_heads: 1
  interaction_dropout: 0.1
  gate_hidden_dim: 64
  gate_dropout: 0.1

  use_interaction: false
  gate_mode: fixed_avg
  use_residual_logit_correction: false