runtime:
  device: cuda
  mixed_precision: fp16
  num_workers: 4
  pin_memory: true
  compile_model: false

seed: 42

logging:
  level: INFO
  log_every_n_steps: 20

checkpoint:
  save_best_only: true
  monitor: macro_f1
  mode: max