benchmark:
  task: text

  experiments:
    - configs/text/experiments/phobert_base_ce.yaml
    - configs/text/experiments/phobert_base_ce_ls005.yaml
    - configs/text/experiments/phobert_base_ce_dropout020.yaml

  seeds: [42, 52, 62, 72, 82]
  device: cuda

  output_root: outputs/runs/text_light_regularization_tuning
  report_dir: outputs/reports/tuning/text_light_regularization

  skip_existing: true