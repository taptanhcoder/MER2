benchmark:
  task: text

  experiments:
    - configs/text/experiments/phobert_base_ce.yaml
    - configs/text/experiments/phobert_base_ce_stable.yaml

  seeds: [42, 52, 62, 72, 82]
  device: cuda

  output_root: outputs/runs/text_stability
  report_dir: outputs/reports/stability/text

  skip_existing: true